# ────────────────────────────────────────────────────────
# 範本：複製此檔為 automation/.env 後填入你的金鑰
# 實際的 .env 檔已被 .gitignore 擋住，不會 commit
# ────────────────────────────────────────────────────────

# Google Gemini API（給 evaluator 和 brief_generator 用）
# 申請：https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Claude API（如果你有 Agent 需要直接呼叫）
# 申請：https://console.anthropic.com/
CLAUDE_API_KEY=your_claude_api_key_here

# ────────────────────────────────────────────────────────
# 可選：覆蓋預設路徑（不填會用 repo 內相對路徑）
# ────────────────────────────────────────────────────────

# Google Sheet（題材庫）
# KVAI_SHEET_ID=your_sheet_id_here
# KVAI_SHEET_NAME=Sheet1

# Pipeline 根目錄（預設 ./pipeline）
# KVAI_PIPELINE_DIR=/absolute/path/to/pipeline

# 自動化根目錄（token.json / credentials.json 放這）
# KVAI_AGENT_DIR=/absolute/path/to/automation

# 活動對照檔（預設 ./knowledge/kv-key-activities.md）
# KVAI_ACTIVITIES_FILE=/absolute/path/to/kv-key-activities.md
